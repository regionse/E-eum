# 잇다 기술 스택 — 전개 과정 전부

2026-08-06. **코드에서 직접 읽은 것만.** 줄 번호는 `itda_core.py` 기준(달라지면 그때 코드가 정답).
「우리 이거 씁니다」라고 말하기 전에 이 문서에 있는지 확인할 것.

---

# 0. 지형

| 축 | LLM | 프레임워크 | 검색 |
|---|---|---|---|
**잇다** | Gemini 3.1 Flash-Lite · **REST 직접**(`urllib.request`) | 없음 | Pinecone + MySQL FULLTEXT |
**덜다** | Gemini | **LangGraph** | Pinecone |
**나누다** | 안 씀 | — | 카카오 로컬 API |

⚠ 「우리는 LangGraph를 씁니다」 → **덜다만.**
⚠ `google-genai` SDK가 requirements에 있지만 **잇다는 안 쓴다.**

### 파일 구성 (`app/itda/`)
| 파일 | 줄수 | 하는 일 |
|---|---|---|
`itda_core.py` | ~5,500 | 엔진 전부 — 슬롯·게이트·검색·카드 |
`match.py` | ~530 | Pinecone·FULLTEXT·RRF·리랭커 |
`controllers.py` | ~570 | 세션·이력·요약접기·지도 저장 |
`prompts.py` | — | GATE_ONTOPIC · SYSTEM |
`schemas.py` | — | 요청/응답 Pydantic |
`router.py` | — | 8개 엔드포인트 |
`session.py` | — | 메모리 LRU + MySQL write-through |
`gemini_util.py` | — | 키 1개 · 429/5xx 1회 재시도 |
`db.py` · `env.py` | — | async 엔진 · .env 로더 |
`sync_runner.py` | — | 배치 서브프로세스 실행기 |

### 엔진 메서드 39개 (`ItdaEngine`)
```
호출층    gemini · turn · think · llm_pick · summarize · utt_kind · _on_topic
판정      crisis_who · harm_who · _vote · _is_spread · _spread_entropy · _cluster_entropy
          _rr_ok · _kw_winner · _named_job · _named_entity · _answered_already
질의      turn_schema · _cache_key · _is_cond_query · _slot_queries · _search_query
DB        _job_by_code · _next_exam · _course_covered · _certs_for · _lcls_of
          _codes_in_lcls · _siblings
계층      _spread_lcls · _shares_word · _other_interest
메모리    fold_facts
본체      step(래퍼) · _step(856줄) · search(674줄) · _add_policy
```

---

# 1. 데이터 (2026-08-06 DB 실측)

⚠ **주석 두 곳이 직업 수를 「613종」이라 적었는데 틀렸다** (`controllers.py:239`,
`itda_core.py:2155`). 613은 **자격증** 수. 실측은 `match.py:12`.

| 무엇 | 규모 | 비고 |
|---|---|---|
**직업** | **1,094** | NCS. 임베딩 본문 ~21자(「직업명·중분류」) → **하이브리드의 이유** |
**강좌** | **8,371** | K-MOOC |
**자격증** | **613** | 큐넷. `entry_note` ⚠ **오염 확인** |
**시험일정** | **2,655** | 접수시작/마감 · 필기 · 실기 · 합격발표 |
**직업 태그** | **1,064 태깅** | `act_type` · `obj_type` · 30개 미태깅 |
**직업 계층** | 3층 | `job_lcls_name`(24) / `job_mcls_name` / `job_scls_name` |

**Pinecone** 인덱스 1개 `eum-itda`, 네임스페이스 3개 · `gemini-embedding-2` **3,072차원** · 코사인

---

# 2. 요청 진입

```
[0] 회원가입·로그인      bcrypt 해싱 · PyJWT · birthdate 수집(잇다는 아직 안 씀)
[1] 잇다 진입            React 18 / Vite 6 / react-router-dom
                         localStorage["eum_itda_chat_{uid}"] = {sid, msgs, mapped}
[2] 첫 인사              _ASK_Q['관심분야'] — LLM 0회
```

```
[3] POST /itda/message
     ├ Pydantic     session_id(min 1) · message(1~2000자)
     ├ JWT          Depends(get_current_user)
     └ _claim_session(st, user_id)   ← 남의 session_id 로 슬롯 훔쳐가기 차단

[4] session.load(db, sid)
     ├ 메모리 LRU dict (_MAX_SESSIONS=5000, pop→재삽입으로 LRU)
     └ 없으면 MySQL itda_session 에서 복원   ← 서버 재시작 생존
```

---

# 3. `_step()` 전개 — 관문 26개, 순서대로

> 위에서부터 차례로 걸린다. **먼저 걸리면 아래는 안 본다.**

### ㉠ 4723 — 거부 감지 (LLM 앞)
```python
_all_no  = none_of_these(user_msg)          # 「다 아니다」 「이 중에 없어요」
_rejected = rejects_last_card(user_msg) or _all_no
```
걸리면 세기를 나눠 기록:
| | 대상 | 강도 | 상한 |
|---|---|---|---|
`_exclude` | 카드로 나갔던 직업(`_last_job`) | **하드 배제** | 8 |
`_demote` | 좁히기 칩으로 보여준 것(`_narrow_codes`) | **강등** | 8 |

그리고 `세부관심`·`_narrowed`·`_narrow_opts`를 비운다.
⚠ **관심분야·활동유형은 안 지운다** — 지우면 대화를 처음부터 다시 한다.

### ㉡ 4758 — 정책 안내 거절
`_policy_offered` 후 1턴 안에 거절하면 `_policy_declined` → 다시 안 권한다.

### ㉢ 4770 — 남용 종료 **← LLM 호출 0회**
```python
_WARN_N, _STOP_N = abuse_limits(profile)     # 턴 비례. 바닥 3/6 → 상한 10/20
if profile['_abuse'] >= _STOP_N: return blocked
```

### ㉣ 4774 — `pre_check()` 4종 판정
```python
meaningful = re.sub(r'[^가-힣a-zA-Z]', '', msg)
_JAMO_OK = {ㅎㅇ ㅇㅇ ㄴㄴ ㄱㅅ ㅇㅋ ㅂㅂ ㅅㄱ ㅈㅅ}       # 축약은 통과
if _bare in _JAMO_OK: return None
if not meaningful or re.fullmatch(r'[ㄱ-ㅎㅏ-ㅣ\s]+', msg): return 'VAGUE'

flat = msg.replace(' ','')          # 공백만 제거
norm = _norm_evade(msg)             # 구두점·숫자·기호·이모지까지 제거 + 반복 축약
def _hit(words): return any(b in flat or b in norm for b in words)

if _hit(SELF_HARM)      and not _is_career_not_crisis(flat): return 'SELFHARM'
if _hit(HARM_OTHERS) or _hit(_SEXUAL_HARM):
    if not (_is_harm_career(flat) or _is_idiom_not_harm(flat)): return 'HARM'
if _sexual_kind(msg, norm) and not _is_harm_career(flat):     return 'UNSAFE'
if ABUSE 걸림: return 'UNSAFE' if _is_abuse_at_bot(msg, norm) else None
```

**4775 VAGUE** → `blocked` 「혹시 오타가 있었을까요?」 · LLM 0회
**4781 SELFHARM** → **차단 안 함**. `crisis_who()` 2층 판정
```
'본인'|'제3자'|'예방·직업관심'|'불명'   → 실패 시 '본인'(가장 보수적)
'예방·직업관심'이면 정상 대화로 계속
그 외는 kind='ask' + 상담 연락처 + 대화 유지
⚠ 남용 카운터에 «절대» 안 센다
```
**4801 HARM** → `harm_who()` 2층
```
'가해' → blocked
'피해'·'제3자' → ask + ☎129 + 나누다(/share/map) 인계
```
**4822 UNSAFE** → `blocked`

### ㉤ 4838 — 좁히기 칩 선택 (`ordinal_of`)
「첫 번째요」 「그거요」 같은 순서·번호 답을 받는다. 다중 선택도 처리.
→ `_job_by_code()` → 카드

### ㉥ 4883 — 인젝션 **← LLM 0회, `bump_abuse(injection)`**

### ㉦ 4912 — 주제 이탈 게이트 **← LLM #1**
```python
if (not has_interest(profile) and _turns <= 1
        and not tells_situation(user_msg)
        and not is_meta(user_msg) and not await self._on_topic(user_msg)):
    return redirect
```
`UTT_KIND=1`이면 `utt_kind()` 6분류, `0`이면 `_on_topic()` 이진 (롤백 경로).

### ㉧ 4942 — 자격증 보유 주장 (`claims_credential`)
### ㉨ 4965 — 시험 일정 질문 (`asks_exam` + `_last_certs`) → DB 값 그대로
### ㉩ 4975 — 뜻 묻기 (`asks_meaning`, 24자 이하) → NCS 설명 220자

### ㉪ 4994 — **`turn()` = LLM #2** (본 호출)
```python
cfg = {'responseMimeType':'application/json',
       'responseSchema': PROFILE_SCHEMA, 'temperature': 0.0}
payload['systemInstruction'] = SYSTEM        # 고정부 분리 → 캐시 적중
```

**받는 것:**
```
슬롯 6개    관심분야·활동유형·다루는대상 (다중값, {값,근거} 쌍)
            세부관심·강점성향 (단일)  ·  제약 (다중값)
action      DIRECT | ASK | CLARIFY | SEARCH | OFFRAMP | REDIRECT | META
query       검색 질의
query_alts  같은 뜻 다른 표현 2개        ← RAG-Fusion 재료, 추가 호출 없음
reply       답변 문장
```

**보내는 맥락:**
```
[앞선 대화에서 확인된 사실]   _facts    ← 코드가 append-only 누적, 재압축 안 함
[앞선 대화 요약]              _summary  ← LLM이 매번 다시 씀
[최근 대화]                   원문 8턴 (HISTORY_TURNS)
[지금까지 파악한 것]          슬롯 ('_' 접두는 제외)
```

**턴 캐시 키** = `[슬롯, 정규화발화, 직전질문(80자), sha1(요약+사실+이력)[:16]]`
⚠ 이력 해시를 빼면 **다른 사용자의 답이 그대로 나간다**(실제로 있었던 사고).

**실패 처리 (5023):** finishReason이 안전차단이면 안내, `_OUR_FAULT`(MAX_TOKENS·
RECITATION·OTHER·LENGTH·MALFORMED_FUNCTION_CALL)면 「다시 말씀해 주세요」.

### ㉫ 5036 — 슬롯 후처리 **(코드만, LLM 0회)**

```
① verify_slots(t['profile'], user_msg)
     {값, 근거} 에서 «근거»가 발화에 실제로 없으면 그 슬롯을 버린다
     = Extractive grounding. 모델의 지어내기를 코드가 막는다

② _NOT_INTEREST 필터 (5040)
     알바·아르바이트·정규직·계약직·투잡·취업·직장·일자리·돈·월급·아무거나·노동
     → 관심분야에서 «항목별로» 제거 (「알바」만 빼고 「편의점」은 남긴다)

③ slot_subtract (5052)   ★ DELETE 연산
     ㉮ 지목 제거 : 부정만 있는 턴 + 쌓인 값이 발화에 그대로 → 뺀다
     ㉯ 방향 전환 : 전환 표지 + 같은 슬롯에 새 값 → 옛 값을 비운다
     ⚠ 제약 슬롯은 대상이 아니다 (원래 부정형으로 오는 게 정상)
     ⚠ 긍정이 섞이면 아무것도 안 뺀다 (「좋은데 힘들어요」는 조건이지 취소가 아니다)

④ merge (5056)
     다중값은 «쌓는다» (≤4). 덮어쓰면 앞말이 사라진다
     그리고 들어온 축에 _slot_src = 'user' 표시

⑤ fill_object_slot (5058)
     다루는대상이 비었을 때만 코드가 채운다
     ㉮ 발화 표지 우선  ㉯ 없으면 활동유형에서 유도
     _slot_src = 'code'  ← can_land 무게 계산용
     ⚠ 부정이 섞인 턴은 건너뛴다

⑥ note_slot_turns — 축이 «몇 턴째에» 찼는지 기록 → one_shot_land 용

⑦ _unsure 리셋 (5072)
     ASK_ORDER + 세부관심이 «실제로» 찼을 때만 초기화
     제약·강점성향만 들어온 턴에는 사다리를 안 되돌린다
```

### ㉬ 5090~5137 — META 처리
```
5090  모델이 놓친 짧고 명백한 메타 발화를 코드가 확정
5098  「모르겠다」인데 META 로 잡혔으면 ASK 로 되돌린다
5100  META 확정 → 답변 조립
        asks_understanding + 카드 있음 → 「~ 말씀하셨어요」 되짚기
        asks_understanding + 카드 없음 → 슬롯을 «사람 말»로 풀어서
        그 외                        → _MET_LABEL 화이트리스트
      ⚠ 내부 슬롯 이름(관심분야 ['용접'])이 화면에 나가면 안 된다
```

### ㉭ 5141 — 코드 DIRECT 탐지
발화가 DB 실재 직업/자격증 이름을 통째로 담으면 **모델이 ASK를 내도** DIRECT로.

### ㉮ 5159~5225 — **착지 가드 (양방향)**
```python
_new_cond = bool(as_list(new_slots.get('제약')))     # 이번 턴에 제약이 왔나

5159  if act=='SEARCH' and not can_land(profile):        act='ASK'   # 성급한 추천 차단
5191  elif act=='SEARCH' and _landed
        and not wants_land and not rejects_last_card
        and not _new_cond:                              act='ASK'   # 재검색 억제
5196  elif act=='ASK' and can_land and not rejects_or_questions
        and not NO_FORCE_LAND and not one_shot_land
        and not _landed:                                act='SEARCH' # 강제 승격
5222  if _all_no and act=='ASK' and can_land
        and not asks_meaning:                           act='SEARCH' # 「다 아니다」→ 새 후보
```

**`can_land()` — 근거 무게**
```python
axes = [k for k in (관심분야,활동유형,다루는대상) if p.get(k)]
LAND_WEIGHT=0 → len(axes) >= 2                        # 예전 방식
LAND_WEIGHT=1 → w = Σ(0.5 if src=='code' else 1.0)
                need = 1.5 if _turns >= 12 else 2.0
                return w >= need
```

**`one_shot_land()`** — 축이 «전부 이번 턴에» 찼으면 착지를 한 번 미룬다.
(「빵 만드는 거 좋아해요」 한 마디에 [제빵] 카드가 나가던 것)

### ㉯ 5232 — 착지 요청 (`wants_land`)
「이제 됐으니 알려줘」. 슬롯이 하나라도 있으면 SEARCH로, 없으면 되묻는다.

### ㉰ 5270~5306 — 답변 문장 다듬기
```
guided       칩을 준 턴이면 ASK 유지
pure_unsure  순수 「모르겠다」 → _UNSURE_STEPS 사다리
5300         ASK 인데 '?' 가 없으면 → ask_reply() 로 «진짜 질문»을 붙인다 (막다른 답변 방지)
5306         NO_FORCE_LAND 켜져 있으면 「이제 찾아볼 수도 있어요」만 알린다
```

### ㉱ 5311 — SEARCH / DIRECT → `search()` 로

```
5343  _bad_ans     모델 질의가 쓸모없으면 폐기
5368  _only_ctx    맥락만 있는 질의 처리
5409  asked_land   착지 요청이면 no_narrow (좁히기 건너뛰고 바로 답)
5432  THINK        분야(lcls)를 모르면 큰 모델로 한 번 «생각» → 그 결과로 검색
5444  THINK(stale) 이미 알면 백그라운드로 갱신, 이번 턴은 안 기다린다
```

### ㉲ 5478~5545 — `search()` 결과 분기
| 반환 | 무엇 | 응답 |
|---|---|---|
`narrow` | 후보가 흩어짐 | 칩 2~5개로 되묻기 |
`need_probe` | 확신 없음 + 되물음 생성됨 | 그 질문 |
`low_score` | 점수가 낮음 | 되묻기 |
`not_found` | 실재하지 않는 목표 | 「우리 목록엔 없어요」 |
`card` | 확정 | 카드 + `_landed=True` + `_last_job` 기록 |

---

# 4. `search()` 전개 — 22단계

### ① 3779 — 질의 없으면 종료
### ② 3804 — 질의 목록 만들기 (`_slot_queries`)
```python
out = []
anchor = _join(slot_text(iv), detail, slot_text(av), obj, strong)   # 누적 슬롯 = 닻
if anchor: out.append(anchor)
if len(iv) > 1:  out += [_join(x, detail, ...) for x in iv[:3]]
elif len(av) > 1: out += [_join(..., x, ...) for x in av[:3]]
```
+ 모델의 `query` · `query_alts` 2개 → **최대 6개 질의**

### ③ 3813 — 조건 질의 제외 (`DROP_COND_Q`)
```python
_COND_NOUN = (체력·시간·아침·야간·주말·돈·학원비·학력·고졸·오염·기름때·냄새·소음·거리…)
_COND_MARK = (지않는·않아도·피할·적은·없이·부담·어려·힘든·쾌적·불가능·못하·제한…)
→ 둘 «다» 있어야 조건 질의로 본다
⚠ 전부 조건이면 그냥 쓴다 (빈 검색보다 낫다)
```

### ④ 3831~3838 — 실제 검색
```
q 없음        → 슬롯 질의만
lcls 있음     → LCLS_FILTER: 그 대분류 안에서만
그 외         → 전체
```
각 질의마다 **벡터 + 키워드**를 돌리고 **RRF(k=60)**로 합친다.

```
벡터(dense)                          키워드(sparse)
  gemini-embedding-2 · 3072d           MySQL FULLTEXT (ngram)
  Pinecone ns=job · 코사인              NATURAL LANGUAGE MODE
  뜻에 강하고 이름에 약함                색인 ft_job_name — 정확한 이름에 강함
```
> **RRF 실측**: k=60이면 1/61과 1/66이 거의 같다 → **사실상 「표 세기」.**
> 가중 RRF를 시도했으나 순위가 안 바뀌어 되돌렸다.

### ⑤ 3842 — `_exclude` 제거 · ⑥ 3856 — `_demote` 강등
⚠ 강등 후 남는 게 2개 미만이면 되살린다(전멸 방지).

### ⑦ 3883 — **job_attr 태그 가중**
```python
_m = (1 if j['act_type'] in _acts else 0) + (1 if j['obj_type'] in _objs else 0)
_ranked.append((_i - _m * ATTR_LIFT, _i, j))     # 순위만 당긴다. 안정 정렬
```
**왜** — NCS는 「제공 서비스 성격」, 사용자는 「무엇을 하는가」:
```
일상생활기능지원 = 사회복지     사용자에겐 전부 「돌봄」인데
가사지원         = 청소         NCS는 세 대분류로 흩어놨다
요양지원         = 보건
```
⚠ **score를 안 건드린다** — score는 `_is_spread`·`_kw_winner`·`JOB_MIN_SCORE`·
엔트로피가 전부 쓰는 값. 가산하면 착지 판정이 통째로 흔들린다.
⚠ 약하게 준다 — 「돕기·돌봄」 칸 31개에 **골프캐디·항공객실서비스·경호**가 섞여 있다.

### ⑧ 3900 — `JOB_CAND_POOL`(8)로 자르기
### ⑨ 3910 — `RERANK_FIRST` (기본 꺼짐, 미검증)
### ⑩ 3953 — `ITDA_MEASURE` 계측 (Query Performance Prediction 문헌 기반, 동작 안 바꿈)

### ⑪ 3997 — DIRECT인데 점수 미달 → `not_found`
`JOB_MIN_SCORE = 0.45` (직업 벡터가 얇아 자격증 0.70보다 낮게)

### ⑫ 4006 — 리랭커 기준 전부 무관 → `not_found`

### ⑬ 4032 — **좁히기 (narrowing)**

**흩어졌나 여부는 `_is_spread()` 가 «2단»으로 정한다** (2026-08-06 — 그 전엔 1단이었다):

```
1단  TOP%   1위 «묶음»(NCS 중분류)의 점유율
       TOP% < 0.90  →  방향이 안 정해졌다  →  «방향 칩»
                          「돌봄 / 만들기 / 사무 중 어디?」
       TOP% ≥ 0.90  →  방향이 정해졌다 (한 동네) → 2단으로

2단  V-ent  그 동네 «안»에서 점수가 벌어졌나
       V-ent ≥ 0.70  →  «세부 칩»   「CO₂용접 / 로봇용접 / 피복아크용접 중 어디?」
       V-ent < 0.70  →  카드(착지)
```

**두 되묻기는 다른 질문이다.** 앞엣것은 「어느 동네」, 뒤엣것은 「그 동네 어디」.
하나로 뭉치면 「용접 하고 싶어요」에 세부를 못 물어보게 된다 —
1단 판정만 쓰던 때 「용접」·「자동차 정비」가 V-ent 0.887·0.964 로 «방향 칩»으로 빠졌다.

**왜 클러스터 «엔트로피»가 아니라 TOP% 인가**
엔트로피는 「묶음 개수」와 「균등성」을 **섞어서** 잰다 → 숫자의 뜻이 안 고정된다:
```
묶음 3개  C-ent = 0.015   「빵 만드는 일」            ← 같은 묶음 수인데
묶음 3개  C-ent = 0.952   「사람에게 도움이 되는 일」   ← 63배 차이
묶음 2개  C-ent = 0.123 ~ 0.317   ·   묶음 4개  C-ent = 0.270 ~ 0.863
```
TOP% 는 「1위 동네가 얼마나 지배적인가」 **하나만** 재서 0.90 이 「90% 이상 한 동네」로 고정된다.
(`_cluster_entropy` 는 남겨 두고 `[measure]` 계측에서만 쓴다)

⚠ **TOP% 는 선택을 하지 않는다.** 어느 후보를 고를지는 뒤의 `llm_pick` 이 한다.
  여기서 정하는 건 「물어볼까 말까」와 「무엇을 물을까」뿐이다.

**알려진 한계 두 개**

① **NCS 분류가 「돌봄」을 세 대분류로 찢어놓는다**
```
일상생활기능지원   사회복지·종교 / 사회복지
가사지원          경비·청소     / 청소      ← 다른 대분류
요양지원          보건·의료     / 보건      ← 또 다른 대분류
```
사용자에겐 전부 「어르신 돌봄」인데 TOP% 가 낮게 나온다. **대분류로 올려도 안 고쳐진다.**
임베딩으로 묶는 것도 실패 — 요양지원↔제빵 0.742 > 아이돌봄↔요양지원 0.720.
⇒ 근본 해법은 `job_attr` 태깅(활동유형·다루는대상). 이미 1,064개 붙어 있다.

② **검색 순위가 틀리면 신호도 못 쓴다** — 「전기 배선」에 내선공사가 안 나오는 건
클러스터가 틀린 게 아니라 **순위**가 틀린 것이다. 별개 문제.

---

**흩어졌다고 판정된 뒤의 후보 고르기:**
```python
picks = [j for j in _pos if j['score'] >= _t - NARROW_BAND]   # 0.03
picks = (picks + 음수점수) [:5]
if rr_done: 리랭커 판정 우선
picks = await self._spread_lcls(db, picks, q)   # 대분류 분배
return {'narrow': True, ...}
```
`_spread_lcls` — 한 계열이 칩을 다 먹지 않게 대분류를 섞는다.
사용자가 지목한 계열이면 그대로 둔다.

### ⑭ 4094 — 점수 낮음 → `low_score`
### ⑮ 4102 — **평탄 판정** — 점수의 «높이»가 아니라 «간격»
```python
_sc = 상위 FLAT_TOP_K(5)
if _gap < FLAT_SPREAD (0.025): 평탄 → 못 고른다
```

### ⑯ 4126~4153 — 후보 확정
```python
if forced:            chosen, reason = forced, '말씀하신 걸 바로 찾아봤어요.'      # 근거 0
elif named:           chosen, reason = named,  '말씀하신 일이에요.'               # 근거 0
elif _kw_winner(jobs):  키워드 압승 (KW_WINNER=5.0, JOB_MARGIN=0.02)
    chosen, reason = jobs[0], '말씀하신 것과 가장 맞닿은 일이에요.'                # 근거 0
else:                 리랭커 → LLM_PICK                                          # 근거 ○
```
⚠ **경로마다 이유 품질이 갈린다.** LLM_PICK만 제대로 된 설명이 나온다.

### ⑰ 4222 — **`llm_pick()` = LLM #3**
후보 `LLM_PICK_N`(3)개 · 설명 `LLM_PICK_DESC`(220자)까지 넣어 최종 선택 + 이유 + 되물음 생성.

### ⑱ 4241 — **충분 판단은 뽑기와 별개**
`_judged` — 「이걸로 답이 되나」를 따로 본다.

### ⑲ 4253 — **코드가 한 번 더 본다**
```python
if _rr < RR_LOW_CONF (0.13): 저확신 표시
```
**왜 모델에게 안 묻나** — 실측에서 5회 중 1회만 `false`.
CLAMBER는 CoT·few-shot이 과신을 키운다 하고, COARSE는 사람도 모델도 「적당히 괜찮은」 답을 수용한다.
**근거**: SOCcer v2(Russ 2023) — *"scores of 0.50 and 0.75 predicting 51% and 70% agreement"*
**0.13의 출처**: n=52 실측에서 NCS에 있는 질의의 **p25 = +0.135**

### ⑳ 4270 — **적응적 종료** — 확신 없고 되물음이 생성됐으면 `need_probe`
### ㉑ 4293 — **계층 백오프** — 확신 없으면 중분류로 물러선다
```python
if _grp: job_reason = f'지금까지 얘기로는 「{_grp}」 쪽이 가까워 보여요. ' + reason
else:    job_reason = '아직 얘기가 짧아서 확실하진 않지만 — ' + reason
```

### ㉒ 4323~4404 — 카드 조립
```
_certs_for(k=N_CERTS, hurry)     자격증 ≤3 · 「지금 바로」 우선 정렬
  └ _next_exam(cert_id)          ① 접수 열린 회차 ② 시험일 미래 ③ 가장 최근
                                 ⚠ 한국 날짜를 코드가 계산 (RDS는 UTC라 밤에 하루 밀림)
_course_covered(job_code)        course_covered / course_cov_vote
  └ no_cert_path=True            → 자격증 대신 «국민내일배움카드» 안내
match_courses(COURSE_POOL=12)    → 리랭커 → 3개  (COURSE_MIN_SCORE=0.70)
hire_url                         고용24(HRD-Net) 검색 링크
_siblings / alternatives         forced 경로엔 후보가 없으므로 중분류 형제를 대안으로
```

---

# 5. 출력 검사 (`step()` 래퍼, 4596~4661)

| 순서 | 기법 | 하는 일 |
|---|---|---|
1 | `drop_echo` | 봇이 자기 앞말을 반복하면 제거 (**자카드 0.55**) |
2 | `drop_questions` | 「기다려 주세요」류 대기 문구 제거 |
3 | `scrub_output` | 내부 슬롯 이름 · **응시요건 단정** 등 위험 문장 제거 |
4 | `fix_name_josa` | 직업명 뒤 조사 교정 (「용접이/용접가」) |
5 | `_add_policy` | 정책 신호가 있으면 덜다 인계 한 줄 덧붙임 |

⚠ `_code_written=True`인 답변은 건드리지 않는다(코드가 쓴 문장은 이미 안전).

---

# 6. 응답 이후 (`controllers.py`)

```
183  _last_ask = reply[:300]              다음 턴 지시대명사 해석용
189  hist.append({r:'u', t: msg[:300]})   ⚠ 사용자 장문도 300자로 잘린다
195  len(hist) > SUMMARIZE_AFTER*2 →
       overflow = hist[:-keep]
       asyncio.create_task(_fold(...))   ← 백그라운드. 사용자는 안 기다린다
         summarize() = LLM #4
           반환 (요약문, 사실목록)
           fold_facts: 중복 제거 + 앞 FACTS_HEAD(12) + 뒤 FACTS_TAIL(18) = 30 상한
222  _think_stale →
       asyncio.create_task(_rethink(...)) ← 큰 모델로 관심대분류 갱신, 다음 턴 반영
280  await session.save(db, sid, st)      MySQL UPSERT
```

### 인계 2방향
| 신호 | 어디로 | 라우트 |
|---|---|---|
정책·지원 문의 / OFFRAMP | **덜다** | `/welfare/policy` |
기관·시설 필요 (폭력 피해) | **나누다** | `/share/map` |

⚠ **대화를 끊지 않는다.** 넘기고 끝내는 게 아니라 한 줄 덧붙인다.

---

# 7. 상수 전부

### 검색·순위
```
JOB_CAND_POOL   8       직업 후보 수
JOB_MIN_SCORE   0.45    DIRECT '없음' 판정선
JOB_NARROW_GAP  0.04    1위가 3위를 이만큼 앞서면 뚜렷한 승자 → 안 좁힘
KW_WINNER       5.0     키워드 압승 판정
JOB_MARGIN      0.02
NARROW_BAND     0.03    좁히기 후보 대역
FLAT_SPREAD     0.025   평탄 판정 간격
FLAT_TOP_K      5       평탄을 볼 상위 개수
ATTR_LIFT       2       태그 가중 폭 (0이면 끔)
COURSE_POOL     12      리랭커에 넘길 강좌 후보
COURSE_MIN_SCORE 0.70
```

### 확신
```
RR_MIN          0.05
RR_LOW_CONF     0.13    ← n=52 실측 p25 = +0.135
RR_PICK_MIN/MAX 2 / 5   ← COARSE·SOCcer 실무 범위 4~6
SPREAD_K        6       엔트로피를 볼 상위 후보
SPREAD_T        0.02    softmax 온도 (간격 0.005~0.16과 같은 자릿수)
SPREAD_H        0.70    2단 문턱 — 한 동네 «안»에서 갈리나
                        ← 0.95는 「거의 아무것도 안 막는다」가 실측으로 드러남
CTOP_H          0.90    1단 문턱 — 1위 묶음 점유율. 구체 질의 0.90~1.00 / 막연 0.34~0.50
TWO_STAGE       1       2단 판정 (0이면 예전대로 V-ent 단독)
SPREAD_MODE     entropy | gap
```

### 대화·메모리
```
MULTI_SLOTS     관심분야·활동유형·다루는대상·제약
MULTI_MAX       4
HISTORY_MODE    'full'  (= 사실+요약+최근 8턴. 전체 대화가 아니다)
HISTORY_TURNS   8
SUMMARIZE_AFTER 20      ⚠ 접기 주기 = AFTER − TURNS = 12턴
FACTS_HEAD/TAIL 12 / 18
LLM_PICK_N      3
LLM_PICK_DESC   220
PICK_PROBE_MAX  60
TEMP_TURN       0.0
SELF_CONSISTENCY 1      ← 꺼짐. 효과가 안 나왔다
QUERY_CACHE_MAX 2000
```

### 착지·남용
```
LAND_W_USER     1.0     사용자가 말한 축
LAND_W_CODE     0.5     코드가 추론한 축
LAND_NEED       2.0
LAND_NEED_LATE  1.5     LAND_RELAX_AFTER(12)턴 뒤
ABUSE_WARN/STOP 3 / 6   바닥값
ABUSE_*_MAX     10 / 20 상한
```

### .env
```
ITDA_UTT_KIND · SLOT_MULTIQ · DROP_COND_Q · SLOT_SUB · LAND_WEIGHT · SESSION_DB
ITDA_HISTORY_TURNS · SUMMARIZE_AFTER · SPREAD_MODE · SPREAD_H · ATTR_LIFT
ITDA_LLM_PICK · CRISIS_TRIAGE · NO_FORCE_LAND · RERANK_FIRST · MEASURE
ITDA_THINK_MODEL / LEVEL / BUDGET · RERANK_MODEL · JINA_RERANK_MODEL
COURSE_LLM_MODEL · COURSE_EMBEDDING_MODEL · COURSE_EMBEDDING_DIMENSION
```
⚠⚠ **로더가 줄 끝 주석을 안 자른다.** `KEY=1  # 설명` → 값이 `'1  # 설명'`이 되어
**조용히 꺼진다.** 설명은 줄 «위»에. (2026-08-06에 실제로 밟아 `UTT_KIND`가 꺼져 있었다)

---

# 8. 데이터 파이프라인 (배치 18개)

```
적재    load_certification · load_cert_detail · load_exam_schedule · load_course
        set_entry_note · set_entry_free
임베딩  embed_jobs · embed_cert · embed_course
산출    build_course_coverage · build_calibration_set · calibrate_threshold
검사    golden_check · golden_ctx · golden_judge · convo_judge
공용    _common.py
```
**`sync_runner.py`** — 관리자 「진로 데이터 최신화」가 **서브프로세스로 순차 실행** + 폴링.
⚠ 배치는 모듈 최상위 실행형(`main()` 없음, import 즉시 실행, `sys.argv`를 최상위에서 읽음,
블로킹 `time.sleep`과 동기 pymysql)이라 **함수로 못 부른다.**

# 9. 검사 도구

| | 무엇 | 비용 |
|---|---|---|
`golden_check.py` | 골든셋 **34케이스** + **코드 단위 90개** | 단위는 0원 |
`checks/fp_test.py` | 정상 발화 오탐 35건 | 0원 |
`checks/evade_test.py` | 우회 표기 32건 | 0원 |
`checks/subtract_test.py` | 슬롯 빼기 14건 | 0원 |
`checks/session_db_test.py` | 세션 DB 11건 ⚠ DROP TABLE 함 | 0원 |
`checks/multiturn_regress.py` | 다중 턴 회귀 8건 (과거 사고 재현) | LLM |
`checks/drift_test.py` | topic drift 6흐름 | LLM |
`checks/land_speed.py` | 착지 속도 8흐름 | LLM |
`checks/holdout.py` · `holdout2.py` | 골든셋과 안 겹치는 검증셋 | LLM |
`checks/personas.md` | 손으로 돌리는 13인 페르소나 | LLM |
`checks/talk.py` | 한 턴씩 대화 + 내부 상태 전부 표시 | LLM |

---

# 10. CRS 용어 매핑

| 잇다 코드 | CRS 정식 명칭 |
|---|---|
슬롯 6축 | **Preference elicitation** |
`{값, 근거}` 스키마 + `verify_slots` | **Extractive grounding / attribution** |
`can_land()` | **Ask-vs-Recommend policy** ← 우리는 «근거 무게» |
narrow 칩 | **Clarification / attribute query** |
`_spread_lcls` | **Diversity in clarification options** |
`llm_pick` | **LLM-as-selector over candidate set** |
`_exclude` / `_demote` | **Negative feedback handling** (하드 / 소프트) |
`_narrow_rest` | **Candidate set maintenance / backtracking** |
`wants_land` | **User-initiated recommendation** |
`slot_subtract` | **Slot DELETE operation** |
`_facts` + `_summary` | **Hierarchical / progressive summarization** |
슬롯 닻 | **Context anchoring** |
`query_alts` + RRF | **RAG-Fusion** |
2단 흩어짐 판정 | **Query Performance Prediction** + **semantic entropy 계열** |
계층 백오프 | **Hierarchical back-off** |
`_certs_for(hurry)` | **Constraint-aware re-ranking** |
카드 + alternatives | **Recommendation list** |
첫 대화 | **Cold-start** ← CRS의 존재 이유 |
직업 **1,094** | **Item space** |
~~`max_turn = 3`~~ | **삭제됨**(2026-08-06) — 전제가 20~100턴 |

# 11. 정확한 이름 + 계보

| 층 | 정확한 이름 | 계보 |
|---|---|---|
대화 관리 | **Frame-based / Schema-guided DST** | GUS(1977) → MultiWOZ(2018) → SGD(2020) |
슬롯 연산 | **Slot operation prediction** (CARRYOVER/UPDATE/**DELETE**) | SOM-DST (ACL 2020) |
LLM 사용법 | **Constrained decoding as information extraction** | 2023~ |
검증 | **Extractive grounding** | attribution 계열 |
검색 | **Hybrid sparse+dense → RRF → cross-encoder** | 현대 IR 표준 |
질의 확장 | **RAG-Fusion** | 2023~ |
최종 선택 | **LLM-as-selector** | LLM reranking 계열 |
확신 판정 | **Score-as-agreement-probability** | **SOCcer v2** (Russ 2023) |
후보 흩어짐 | **Query Performance Prediction** | QPP 계열 |
불확실 시 | **Hierarchical back-off** | 계층 분류 |
도메인 | **Occupational coding** (자유서술 → 표준 직업분류) | **SOCcer** / CASCOT / NIOCCS |

---

# 12. 안 쓰는 것 (오해 방지)

- ❌ **LangChain / LangGraph** — 덜다만
- ❌ **google-genai SDK** — requirements엔 있지만 잇다는 REST 직접
- ❌ **BM25** — MySQL FULLTEXT다 (비슷하지만 다르다)
- ❌ 파인튜닝 / 자체 모델 학습 / RLHF
- ❌ TypeScript / 상태관리 라이브러리 / UI 프레임워크
- ❌ Redis — 세션은 메모리 + MySQL
- ⚠ **Self-consistency** — 구현돼 있으나 **기본 꺼짐**. 실측에서 이득이 없었다.
  「다수결 씁니다」라고 말하면 안 된다.
- ⚠ **`RERANK_FIRST`** — 플래그는 있으나 **미검증**
- ⚠ **SCOBot** — SCORM e러닝 라이브러리. **SOCcer**와 혼동 주의.

# 13. 코드에 인용된 문헌

**⚠ 주석에서 옮긴 것. 발표 전 원문 재확인 필수.**

| 인용 | 어디에 |
|---|---|
**Russ et al. 2023** (SOCcer v2) | `RR_LOW_CONF` |
**Josse et al. 2025 / 2026** (SOCcer) | 되묻기 선택지 개수 |
**Kononykhina 2026** (COARSE) | 확신 자기보고 불신 |
**Laban et al. 2025** (2505.06120) | 다중 턴 39% 하락 → 이력 8턴 |
**Dongre et al. 2025** (2510.07777) | drift = 조절 가능한 평형 → 슬롯 닻 |
**Yang et al. 2025** (2509.19700) | topic drift |
**MoNET** (2211.05503) | state momentum |
**Cardoso et al. 2017** | 서술 요구 금지 → 질문을 «고르기»로 |
**Savickas CCI 3번** | `_ASK_Q2['관심분야']` 첫 항목 |
그 외 | 2606.19286 · 2510.21352 · 2501.05584 · 2410.01627 · 2406.18960 · 2401.16659 · 2305.07614 |
