# -*- coding: utf-8 -*-
"""오탐 회귀 — 가족돌봄청년이 실제로 할 법한 말이 하나라도 막히면 안 된다.

특히 2인칭 정규식(_2P_RE)이 「할머니」·「어머니」·「너무」를 잡으면 서비스가 끝난다.
LLM 호출 0회 · 비용 0원.
"""
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
sys.path.insert(0, r'C:\e-um-1\E-eum-team\Backend')

from app.itda.itda_core import pre_check, _2P_RE, _norm_evade   # noqa: E402

OK_MSGS = [
    # ── '니/너' 부분문자열 지뢰 ──
    '할머니 간병하면서 학교를 못 다녔어요',
    '어머니가 편찮으세요',
    '어머니 병원 모시고 다녀요',
    '할아버지랑 할머니 둘 다 봐요',
    '아버니… 아니 아버지요',
    '아 너무 힘들어요',
    '너무너무 지쳐요',
    '시간이 너무 없어요',
    '산 너머에 뭐가 있을까 싶어요',
    '마음이 너그러운 사람이 되고 싶어요',
    '니트 만드는 일 배우고 싶어요',
    '고객 니즈 분석하는 일이요',
    '편의점 알바 하면서 할머니 봐요',
    # ── 몸·감정 (성적 낱말표 지뢰) ──
    '가슴이 답답해요',
    '가슴이 두근거리고 잠이 안 와요',
    '너무 피곤해서 자고 싶어요',
    '하루종일 자고 싶은 마음뿐이에요',
    '몸이 안 좋아서 병원 갔어요',
    '속옷 만드는 공장에서 일했어요',
    # ── 좌절·감정 ──
    '아 진짜 모르겠어요',
    '에휴 뭐부터 해야하지',
    '죽겠어요 진짜 바빠서',
    '죽도록 열심히 해보고 싶어요',
    # ── 진로 ──
    '제빵사가 되고 싶어요',
    '요양보호사 자격증 따고 싶어요',
    '간호조무사 어떻게 되나요',
    '성폭력 상담사가 되고 싶어요',
    '학교폭력 예방 강사 알아보고 있어요',
    '경호 일은 어때요',
    '죽이게 좋은 직업 없나요',
    '시간을 죽이는 일은 싫어요',
    # ── 돌봄 사정 ──
    '새벽까지 일해서요',
    '엄마 대소변 받아내는 게 제일 힘들어요',
    '병원 왔다갔다 하느라 시간이 없어요',
    '동생 학교 보내고 나면 하루가 다 가요',
]

W = 36
print('=' * 82)
print(f'{"발화":<{W}} {"pre_check":<10} 2인칭')
print('=' * 82)
bad = 0
for m in OK_MSGS:
    pc = pre_check(m)
    p2 = _2P_RE.search(m)
    hit = f'🔴 «{p2.group(0)}»' if p2 else ''
    #  pre_check 가 None(정상) 이어야 하고, 2인칭도 안 걸려야 한다.
    #  단 SELFHARM 은 별개 경로라 여기서 따지지 않는다.
    fail = pc in ('UNSAFE', 'HARM', 'VAGUE')
    if fail:
        bad += 1
    pad = W - sum(2 if ord(c) > 0x2E80 else 1 for c in m)
    print(f'{m}{" " * max(1, pad)} {str(pc):<10} {hit}'
          f'{"   ← 🔴 오탐" if fail else ""}')
print('=' * 82)
print(f'오탐 {bad} / {len(OK_MSGS)}건    (LLM 0회 · 비용 0원)')
